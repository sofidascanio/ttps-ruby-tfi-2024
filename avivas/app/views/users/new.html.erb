<% content_for :title, t('new') + ' ' + t('user') %>

<div class="title-container">
  <h1> Nuevo Usuario  </h1>
  <%= link_to t('back_to') + ' ' + t('users'), users_path, class: "add-button"  %>
</div>

<%= render "form", user: @user %>