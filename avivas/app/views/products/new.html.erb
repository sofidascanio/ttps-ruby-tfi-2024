<% content_for :title, t('new') + ' ' + t('product') %>

<div class="title-container">
  <h1>  Nuevo Producto </h1>
   <%= link_to t('back_to') + ' ' + t('products'), products_path, class: "add-button"  %>
</div>

<%= render "form", product: @product %>
