<p style="color: green"><%= notice %></p>

<% content_for :title, "Categories" %>

<h1>Categories</h1>

<div id="categories">
  <% @categories.each do |category| %>
    <%= render category %>
    <p>
      <%= link_to "Ver Categoria", category %>
    </p>
  <% end %>
</div>

<%= link_to "Crear Categoria", new_category_path %>

<br>

<%= link_to "Volver al Inicio", root_path %>